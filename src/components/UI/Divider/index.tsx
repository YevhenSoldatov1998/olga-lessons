import React from 'react';

const Divider = (props: { className?: string }) => {
  return (
    <div {...props} style={{height: 1, width: '100%', backgroundColor: `var(--color-grayscale-c6`}}/>
  );
};

export default Divider;