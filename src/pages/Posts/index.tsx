import React from 'react';
import {Typography} from "components/modules";
import {TypographyVariant} from "../../helpers/types";

const Posts = () => {
  return (
    <div>
      <Typography variant={TypographyVariant.h40}>Hello</Typography>
    </div>
  );
};

export default Posts;